# NeuroTek AI — Website Environment Variables
# Copy to .env.local for local development. Never commit .env.local.

# Backend API URL (used by the Login page to call the real auth API)
# Development:
VITE_API_URL=http://localhost:4000
# Production (set in Vercel dashboard → Settings → Environment Variables):
# VITE_API_URL=https://neurotek-ai-backend.onrender.com
